use crate::{Animal, WinnerTable};

pub fn output_stats(_table: WinnerTable) {
    panic!("fn output_stats(table) not yet implemented.")
}

pub fn get_animal(_animals: Vec<Animal>) -> Animal {
    panic!("fn get_animal(animals) -> Animal not yet implemented.")
}
